name: Release

on:
  push:
    tags: ["v*"]

jobs:
  release:
    uses: FreeSynergy/fs-ci/.github/workflows/release-desktop.yml@main
    with:
      crate-name: "fs-gui-engine-iced"
    secrets: inherit
